# 作業振り返り・フィードバック

## 1. 良かった点
- WSL2 + Ubuntu + VSCode × WSL の環境構築が完了し、開発可能な状態になった
- Python 仮想環境 `.venv` の作成と依存関係インストールが成功
- FastAPI の起動確認ができ、`/health` エンドポイントの疎通確認が完了
- `.env` をプロジェクト直下に移動し、Settings から正しく読み込めるように設定
- ディレクトリ構成の整理ができ、今後の拡張・管理がしやすくなった

## 2. 改善できそうな点
- FastAPI 起動時や VSCode 連携での「おまじない」が多く、毎回の手順がやや煩雑
- `.env` 移動や型指定など Pydantic Settings の扱いでエラーが発生しやすい
- `/docs` へのアクセスや WSL → Windows の接続設定で理解が必要だった

## 3. 次回への提案
- Makefile や簡易起動スクリプトで環境構築・サーバー起動をワンコマンド化
- Settings 設計の確定と環境切替（local / dev / prod）を整理
- Docker 化（3.4）で WSL 環境依存を排除し、起動コマンドを統一
- CI 初期設定（3.5）で Lint / mypy の自動チェックを導入し、環境差異の問題を減らす

## 4. 総括
今回の作業で WSL2 + Ubuntu 環境下での FastAPI 開発環境が整い、
仮想環境や `.env` 設定も安定化しました。
細かい操作や接続周りのトラブルを経験したことで、
今後の Makefile・Docker・CI 導入時にスムーズに作業できる基盤ができています。
