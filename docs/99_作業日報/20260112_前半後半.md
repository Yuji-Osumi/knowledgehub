# 作業振り返り・フィードバック　前半

## 1. 良かった点

* **フロントエンド環境構築を独立タスクとして切り出した判断**

  * UIスタブ作成を「実装」ではなく「検証フェーズ」と定義し直したことで、目的が明確になった。
  * WBS上でも *API実装前の前段工程* として自然に接続できている。

* **Vite + React + TypeScript を最小構成で素早く立ち上げた点**

  * 余計なライブラリを入れず、10分で起動確認まで到達。
  * “まず動くものを作る”という初動として理想的。

* **Tailwind CSS を最小導入に留めた点**

  * デザイン作り込みを避け、className 適用確認のみにフォーカスできている。
  * UIスタブの目的（画面遷移・構造確認）と完全に整合。

* **react-router 導入の粒度が適切**

  * 画面遷移の「骨格」だけを作り、ページ詳細は後回しにした。
  * URL設計検証に集中できる構成になっている。

* **Makefile / README を更新し、起動方法を明文化した点**

  * “動かし方が分かる状態”を成果物として残している。
  * 将来の自分・第三者視点を意識できている。

---

## 2. 改善できそうな点

* **Node / npm 周りの前提が暗黙になっていた**

  * WSL環境では `npm` 未導入に一瞬詰まった。
  * README もしくは docs に「Node.js 前提」の一言があると迷いにくい。

* **Tailwind 導入時の warning の意味整理に少し時間がかかった**

  * “ユーティリティ未使用 warning は初期状態では正常”という知識があれば即判断できた。
  * 今後は「初期警告は想定内か？」を一度言語化してから調査すると効率的。

* **フロントエンド関連タスクの完了条件がやや抽象的**

  * 「確認した」の粒度（起動確認／画面表示確認／URL直接アクセス確認）を
    もう一段だけ明示できると、次回振り返りが楽になる。

---

## 3. 次回への提案

* **UIスタブの“画面箱”を先にすべて揃える**

  * Login / Articles / Detail / Edit を「中身なし」で揃え、
    画面遷移が一周できる状態を作る。
  * ここで初めて「APIで何が必要か」を自然に逆算できる。

* **フロントエンド専用の軽い進捗セクションを継続更新**

  * README に追加した UIスタブ項目を、チェック更新していく。
  * 完成させる必要はなく、「考えた順序」が見えることが目的。

* **Tiptap 導入は“編集画面が固まってから”**

  * 先に入れると目的がズレやすい。
  * 「編集画面で何をしたいか」が見えた段階で導入がベスト。

---

## 4. 総括

今回の作業は、
**「フロントエンドを作った」ではなく
「API実装前に検証すべき土台を整えた」**という点が非常に良い。

* 作る前に目的を再定義できている
* 便利そうな施策（自動URLオープン等）を“今はやらない”と判断できている
* README / Makefile まで含めて成果としてまとめている

これは個人開発でも、
**チーム開発・大規模開発の思考がそのまま適用されている状態**。

この流れなら、
次の UIスタブ作成 → API設計フィードバック はかなりスムーズに進む。

今日は「正しい順番で前に進んだ日」だった。
胸を張っていい内容だと思う。

# 作業振り返り・フィードバック

## 1. 良かった点

* **UIスタブの目的を一貫して守れた**

  * 「バックエンド未接続・画面遷移確認」という前提を崩さず、機能を作り込みすぎなかった。
  * 永続化・高度なUI・エディタ機能などを意図的にスコープ外に置けたのは判断として非常に良い。

* **設計 → 実装 → 検証の流れがスムーズ**

  * 5.1（画面遷移定義）→ 5.2（ダミーデータ）→ UI実装が自然につながっており、手戻りがほぼなかった。
  * 画面遷移・ルーティング設計が先に固まっていたことで、実装が迷わず進んだ。

* **1画面2モード設計（新規/編集）の判断が的確**

  * ArticleEdit / ArticleNew を統合し、重複を排除できた。
  * API設計（POST / PUT）へのフィードバックとしても価値の高い構成になった。

* **モックAPIの粒度が実務レベル**

  * getArticles / getArticleById / saveArticle など、実APIに置き換えやすい形で定義されている。
  * 遅延シミュレーションにより、UXやローディング設計の検証もできた。

* **成果物の言語化が非常に丁寧**

  * 実装内容・設計判断・非対応範囲が明確に整理されており、後から見返しても理解しやすい。
  * そのまま設計ドキュメントやポートフォリオに転用可能なレベル。

---

## 2. 改善できそうな点

* **5.6（API設計へのフィードバック）が未形式化**

  * 内容自体は頭の中・振り返り文書に存在しているが、API仕様としてはまだ未整理。
  * 明日以降、エンドポイント一覧・リクエスト/レスポンス例として落とす必要がある。

* **型定義とAPI契約の対応関係を明示できる余地**

  * Article / Tag / User の型は揃っているので、

    * 「どの画面でどのフィールドが必須か」
    * 「一覧と詳細で返す項目差」
      を整理すると、API設計がさらにスムーズになる。

* **エラー系ケースの洗い出しは最低限**

  * UIスタブとしては十分だが、

    * 404
    * 権限エラー
    * 保存失敗
      の整理は 5.6 で補完したい。

---

## 3. 次回への提案

* **5.6 を“軽く・短時間で”完了させる**

  * 2〜3時間で以下をまとめるのがおすすめ：

    * APIエンドポイント一覧（GET / POST / PUT）
    * Article の作成・更新 payload
    * 認証前提（Cookie/JWT想定）
  * 今回のUIスタブを“正”としてAPIを引き出す形にすると迷いがない。

* **UIスタブは一旦触らない**

  * 追加機能を思いついても、今は封印。
  * UIは「API設計の検証用ツール」と割り切るのが吉。

* **今回の成果を次フェーズにそのまま流用**

  * mockApi.ts → 実APIクライアントに差し替え
  * ArticleFormPage → POST / PUT 実装の検証場
  * ほぼ捨てコードが出ない構成なので安心して進められる。

---

## 4. 総括

* **UIスタブ作成としては“理想的な終わり方”**

  * 目的達成度が高く、スコープ管理も的確。
  * 作りすぎず、しかし浅すぎないバランスが非常に良い。

* **設計力と実装力の両立ができている**

  * コードを書くためのコードではなく、
    「次の設計判断を楽にするための実装」になっている。

* **次フェーズへの地盤は完全に整った**

  * 5.6 を形式化すれば、API設計に迷う要素はほぼ残らない。
  * 明日以降は「考える時間」より「整理する時間」になるはず。
